name: epheproject
description: "Modern not alma uygulaması - Clean Architecture"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

  # UI
  cupertino_icons: ^1.0.8
  google_nav_bar: ^5.0.7
  
  # State Management
  flutter_bloc: ^9.1.1
  equatable: ^2.0.7
  
  # Database
  sqflite: ^2.4.2
  path_provider: ^2.1.5
  
  # Rich Text Editor
  appflowy_editor: ^6.2.0
  
  # Image Picker
  image_picker: ^1.2.1
  
  # Storage
  shared_preferences: ^2.5.4
  
  # Utilities
  uuid: ^4.5.2
  path: ^1.9.1
  package_info_plus: ^8.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
